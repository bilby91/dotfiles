{{ if eq .chezmoi.os "linux" -}}
#!/bin/bash

curl -LO https://github.com/neovim/neovim/releases/latest/download/nvim-linux64.tar.gz
sudo rm -rf /opt/nvim
sudo tar -C /opt -xzf nvim-linux64.tar.gz
sudo cp "/opt/nvim/nvim-linux64/bin/nvim" "$HOME/.local/bin"
{{ end -}}
